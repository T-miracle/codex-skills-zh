interface:
  display_name: "规格拆分工单"
  short_description: "把 plan 或 spec 拆成声明 blocking edges 的 tracer-bullet tickets"
  default_prompt: "使用 $to-tickets 把这个 spec 拆成可执行的 tracer-bullet tickets。"

policy:
  allow_implicit_invocation: false
