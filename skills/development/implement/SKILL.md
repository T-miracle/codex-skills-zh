---
name: implement
description: "根据规格说明（spec）或一组工单（tickets）实现一项工作。"
---

实现用户在规格说明（spec）或工单（tickets）中描述的工作。

尽可能在预先约定的接缝（seams）处使用 /tdd。

定期运行类型检查（typechecking）和单个测试文件（single test files），并在最后运行一次完整测试套件（full test suite）。

完成后，使用 /code-review 进行代码审查（code review）。

将你的工作提交（commit）到当前分支。
